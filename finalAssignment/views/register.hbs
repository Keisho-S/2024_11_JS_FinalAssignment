<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
     <link rel="stylesheet" href="../stylesheets/registerstyle.css">
     <link rel="stylesheet" href="../stylesheets/layoutstyle.css">
    <title>Registration Form</title>
</head>
<body class="RegisterBody">
    <div class="RegisterContainer">
        <h1 class="Register">Register</h1>
        <form  method="post">
            <label for="username">Username: *</label>
            <input name="username" id="username" required type="email" placeholder="email@email.com"/>

            <label for="password">Password: *</label>
            <input  name="password" id="password" type="password" required pattern="(?=.*\d)(?=.*[a-z])(?=.*[A-Z]).{8,}"/>

            <label for="confirm">Confirm: *</label>
            <input name="confirm" id="confirm" type="password" required pattern="(?=.*\d)(?=.*[a-z])(?=.*[A-Z]).{8,}"/>

            <button type="submit">Register</button>
        </form>
        <p class="login-link">Already have an account? <a href="/login" title="Login">Login</a></p>
    </div>
</body>
</html>